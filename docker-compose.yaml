version: '3.9'

services:
  get_the_flaaaaaaaaaa:
    restart: always
    build:
      context: "./challenges/pwn/get_the_flaAAAAAAAAA"
      dockerfile: "./Dockerfile"
    ports: 
      - "50371:1337"
  web-servercheck:
    restart: always
    build:
      context: "./challenges/web/servercheck"
      dockerfile: "./Dockerfile"
    ports:
      - "25870:8080"
  web-charger-blues-first-website-pt1:
    restart: always
    build:
      context: "./challenges/web/charger-blues-first-website-pt1"
      dockerfile: "./Dockerfile"
    ports:
      - "46081:5000"
